name: CI

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches: ["**"]
    paths-ignore:
      - ".github/workflows/**"
      - "**/*.md"
      - "docs/**"
      - ".gitignore"
      - ".env.example"
      - "LICENSE"
  pull_request:
    branches: [dev, main]
    paths-ignore:
      - ".github/workflows/**"
      - "**/*.md"
      - "docs/**"
      - ".gitignore"
      - ".env.example"
      - "LICENSE"

jobs:
  # ── 1. 백엔드 Lint ────────────────────────────────────────────
  backend-lint:
    name: Backend Lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - run: pip install ruff --quiet
      - run: ruff check app/ --output-format=github

  # ── 2. 백엔드 테스트 & 커버리지 ──────────────────────────────
  backend-test:
    name: Backend Test & Coverage
    runs-on: ubuntu-latest
    needs: backend-lint

    env:
      TEST_DATABASE_URL: sqlite:///./test_ci.db
      SECRET_KEY: ci-test-secret

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: 패키지 설치
        run: pip install -r requirements.txt --quiet

      - name: 테스트 실행
        run: |
          mkdir -p reports
          python -m pytest tests/ \
            --junit-xml=reports/test-results.xml \
            --cov=app \
            --cov-report=xml:reports/coverage.xml \
            --cov-report=term-missing \
            -v

      - name: 테스트 결과 업로드
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: reports/

  # ── 3. 프론트엔드 빌드 ────────────────────────────────────────
  frontend-build:
    name: Frontend Build
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: pnpm 설치
        run: corepack enable && corepack prepare pnpm@10.32.1 --activate

      - name: 의존성 설치
        run: pnpm install --frozen-lockfile

      - name: 타입 체크
        run: pnpm tsc --noEmit

      - name: 빌드
        run: pnpm build

  # ── 4. 시크릿 유출 감지 ───────────────────────────────────────
  secret-scan:
    name: Secret Scan (Gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Gitleaks 설치
        run: |
          curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.21.2/gitleaks_8.21.2_linux_x64.tar.gz \
            | tar -xz gitleaks
          sudo mv gitleaks /usr/local/bin/

      - name: 시크릿 스캔
        run: gitleaks detect --source . --redact -v

  # ── 5. Dockerfile 보안 린트 ───────────────────────────────────
  dockerfile-lint:
    name: Dockerfile Lint (Hadolint)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: Dockerfile

  # 참고: SonarQube 정적 분석은 CI 에서 돌리지 않는다.
  # 분석 담당자가 자기 PC 에 `make up-sonar` 로 띄운 로컬 인스턴스에서 `make scan` 수동 실행.
  # 사유: VDI 의 SonarQube 가 reset/다운으로 토큰이 무효화되는 사고가 반복돼 운영 안정성을 위해 로컬 운영으로 전환.
